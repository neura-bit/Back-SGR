# ===== Perfil TEST (entorno de pruebas) =====
spring.datasource.url=${SPRING_DATASOURCE_URL_TEST:jdbc:postgresql://148.230.85.129:5433/UbikaPruebas}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME_TEST:vectordb369}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD_TEST:BJBQj381}

spring.datasource.hikari.auto-commit=false
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=true

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.health.livenessstate.enabled=true
management.health.readinessstate.enabled=true

# File Storage Configuration (test)
file.upload-dir=${FILE_UPLOAD_DIR:/app/uploads}

# Multipart Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
